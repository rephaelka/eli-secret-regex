# =============================================================================
# Functions manifest — tells `crossplane render` which functions to use.
#
# Uses Docker (default runtime) to pull and run function-patch-and-transform.
# Requires Docker to be running locally.
# =============================================================================

apiVersion: pkg.crossplane.io/v1beta1
kind: Function
metadata:
  name: function-patch-and-transform
spec:
  package: xpkg.upbound.io/crossplane-contrib/function-patch-and-transform:v0.8.2
